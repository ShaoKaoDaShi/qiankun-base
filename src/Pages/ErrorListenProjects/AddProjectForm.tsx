import { Form, Button } from "antd";
import React from "react";

const AddProjectForm = (props) => {
    return (
        <Form>
            <Form.Item label="项目名称"></Form.Item>
        </Form>
    );
};
